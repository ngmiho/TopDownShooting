using UnityEngine;

public class Circle2 : MonoBehaviour
{
    //public GameObject bullet;
    //private Vector3 mouse;
    //private void Start()
    //{
    //    mouse = Camera.main.WorldToScreenPoint(Input.mousePosition);
    //    mouse.z = 0;
    //    Destroy(gameObject, 3f);
    //}

    //private void Update()
    //{
    //    Vector3 newMouse = mouse;
    //    float randomNumber = Random.Range(-2.0f, 2.0f);
    //    if (randomNumber > 0)
    //        newMouse.x += randomNumber;
    //    else
    //        newMouse.x -= randomNumber;
    //    Instantiate(bullet, mouse, Quaternion.identity);
    //}
}
