using UnityEngine;

public class StoreUIManager : MonoBehaviour
{
    //public GameObject route2;

    private void Start()
    {
        //Time.timeScale = 1.0f;
    }

    private void OnDestroy()
    {
        //Instantiate(route2, FindObjectOfType<Player>().transform.position, Quaternion.identity);

        //FindObjectOfType<UIManager>().GetComponentInChildren<SpawnMonsterLevel02>(true).enabled = true;
    }
}
